interface AttackInterface {    // This interface will contain functions (like any interface)
    fun yell(){
        println("AAAAAAAAAAAAAAAAAAAHHHH")  // The simplest function in the history of mankind that will only print a scream
    }
    fun transform()     // A function that is only declared here but implemented on the data class because it needs to evaluate values from there
}